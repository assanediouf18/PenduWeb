@page
@model Pendu.Pages.Admin.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PenduModel[0].Secret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PenduModel[0].KnownLetters)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PenduModel[0].NbMistakes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PenduModel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Secret)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KnownLetters)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NbMistakes)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
